# behat.yml

default:
    suites:
        default:
            contexts:
                - FeatureContext
                - MyMinkContext
    extensions:
        Behat\MinkExtension:
            goutte: ~

