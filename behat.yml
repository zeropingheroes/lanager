# behat.yml

default:
    suites:
        default:
            contexts:
                - FeatureContext
                - MinkContext
            extensions:
                Behat\MinkExtension:
                    base_url: http://lanager
                    goutte: ~
