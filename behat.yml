# behat.yml

default:
    suites:
        default:
            contexts:
                - FeatureContext
                - MyMinkContext
    extensions:
        Behat\MinkExtension:
            base_url: http://lanager
            goutte: ~

